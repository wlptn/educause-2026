# TEMPLATE -- do not commit real credentials.
# Only needed for a PRIVATE fork of the content repo. For this public repo, AAP clones
# anonymously and no SCM credential is required.
# Copy to git-scm-credential.yaml and fill in values, or create the credential in the AAP UI.
apiVersion: v1
kind: Secret
metadata:
  name: git-scm-credential
  namespace: aap
type: Opaque
stringData:
  url: "https://github.com/wlptn/educause-2026.git"
  username: "REPLACE_WITH_USERNAME"
  password: "REPLACE_WITH_TOKEN"
