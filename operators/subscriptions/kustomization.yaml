apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# rhdp branch: the RHOAI / Serverless / Service Mesh / Connectivity Link operators
# are already installed on the cluster (and owned by ArgoCD), so only AAP is installed here.
resources:
  - aap-namespace.yaml
  - aap-subscription.yaml
