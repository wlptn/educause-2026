apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# rhdp branch: RHOAI/DSC, Serverless, Service Mesh, Connectivity Link and the GPU stack
# are already installed on the cluster (ArgoCD-owned). This applies only AAP -- the
# unified AnsibleAutomationPlatform CR (Gateway + Controller; Hub/EDA/Lightspeed
# disabled), because the self-service portal requires the AAP Platform Gateway.
resources:
  - ansible-automation-platform.yaml
